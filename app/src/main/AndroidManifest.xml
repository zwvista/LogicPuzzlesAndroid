<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.zwstudio.lightupandroid"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".android.GameApplication">
        <service android:name=".android.MusicService"
                 android:enabled="true"/>
        <activity
            android:name=".android.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".android.GameActivity"
                  android:screenOrientation="portrait">
        </activity>
        <activity android:name=".android.OptionsActivity"
                  android:screenOrientation="portrait">
        </activity>
    </application>

</manifest>